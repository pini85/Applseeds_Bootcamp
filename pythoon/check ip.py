def check_ipv4(s):
    # loop over the string and present the numbers inside them
    numbers = s.split(".")
    print(int(numbers))
    # need to separte them in a list without the dot.
   #first check if the number between 0 -255



print(check_ipv4("250.0.10.44"))
